let arregloNumeroUno: number[] = [1,2,3,4,];


let sumarDosNumeros = (numeroUno: number, numeroDos: number): number => {
    return numeroUno + numeroDos;
}

let potenciaDeUnNumero = numero => {
    return numero * numero;
}

let potenciaDeUnNumeros = numero => numero * numero;

arregloNumeroUno.forEach(
    (valorDeArreglo, indice, arreglo) => {
        console.log(valorDeArreglo);
        console.log(indice);
        console.log(arreglo);
    }
)





