var arregloNumeroUno = [1, 2, 3, 4,];
var sumarDosNumeros = function (numeroUno, numeroDos) {
    return numeroUno + numeroDos;
};
var potenciaDeUnNumero = function (numero) {
    return numero * numero;
};
var potenciaDeUnNumeros = function (numero) { return numero * numero; };
arregloNumeroUno.forEach(function (valorDeArreglo, indice, arreglo) {
    console.log(valorDeArreglo);
    console.log(indice);
    console.log(arreglo);
});
